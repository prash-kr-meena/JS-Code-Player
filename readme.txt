recreate http://jsbin.com/ 

1. except not console or file/add library/logon/blog/help
2. output window updates live as you change text

HTML
CSS
Javascript
Output

Top bar, button fr each, toggles visibility
Panes resizable. resize others when resized, always fill screen width, always full height - ? rearranging ?
  ?? can we do a negative resize with alsoResize??

Output - iframe - can it take text input or just a file?
OR assmble css and js as internal - ? libs (jquery,jqueryui,etc etc - guess only as CDNs - that's ok)